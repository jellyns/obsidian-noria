# Security Policy

感谢你帮助 Noria 提升安全性。Noria 是本地 Obsidian 桌面插件，主要风险集中在 vault 文件读写、插件设置、SecretStorage、外部命令调用和第三方 API key 处理。

## 支持范围

当前仅维护 `main` 分支和最新 GitHub Release。报告问题时，请尽量基于最新 release 或最新 `main` 复现。

## 如何报告

请不要在公开 issue 中披露可利用细节、完整密钥、PoC 或敏感 vault 内容。

优先方式：

1. 使用 GitHub 的 private vulnerability reporting 或 Security Advisory（如果仓库已启用）。
2. 如果不可用，先开一个不含细节的公开 issue，标题标明 `Security`，只说明影响模块和需要私下沟通。
3. 与维护者建立私下渠道后，再提供完整复现信息。

## 建议提供的信息

- 受影响版本或 commit。
- 受影响功能，例如设置页、任务写回、复盘中心、天气密钥、安装脚本。
- 最小复现步骤。
- 期望行为和实际行为。
- 风险等级判断：数据泄露、任意文件写入、命令执行、拒绝服务、密钥暴露等。
- 可行的临时缓解方案。

如果复现需要示例 vault，请使用脱敏样本，不要发送真实私密笔记。

## 响应目标

- 7 天内确认收到并判断是否可复现。
- 14 天内给出修复计划、缓解建议或关闭理由。
- 高风险问题会优先修复并发布 release。

## 披露原则

在补丁或缓解方案可用前，不公开 PoC、关键利用细节或真实密钥。修复发布后，可以在 changelog 中说明风险类型和影响范围，但不展开可直接滥用的步骤。
