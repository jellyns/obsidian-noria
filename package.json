{
  "name": "noria",
  "version": "0.4.6",
  "private": true,
  "type": "commonjs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jellyns/obsidian-noria.git"
  },
  "homepage": "https://github.com/jellyns/obsidian-noria#readme",
  "scripts": {
    "build": "node scripts/sync-home-dashboard-css.mjs && node scripts/build-embedded-runtime.mjs && esbuild src/main.js --bundle --platform=node --format=cjs --target=es2018 --external:obsidian --outfile=main.js",
    "build:release": "npm run build",
    "install:vault": "npm run build && node scripts/install-release-to-vault.mjs",
    "release:check": "npm run build:release && node scripts/release-check.mjs && node scripts/release-smoke.mjs",
    "test": "node --test tests/*.js",
    "check": "npm run build && npm test",
    "version": "node scripts/version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
